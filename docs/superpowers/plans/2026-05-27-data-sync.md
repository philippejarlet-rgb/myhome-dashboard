# Data Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer localStorage par des fichiers JSON côté serveur pour que todos, courses, radios et villes météo soient partagés entre tous les appareils de la famille.

**Architecture:** Une route API dynamique `/api/data/[type]` lit (GET) et écrit (PUT) des fichiers JSON dans `data/`. Chaque page charge ses données depuis l'API au montage et sauvegarde à chaque modification. Migration automatique depuis localStorage au premier chargement si les données serveur sont vides.

**Tech Stack:** Next.js 16 App Router, Node.js `fs` (readFileSync/writeFileSync), route dynamique `[type]`.

---

## Fichiers

| Fichier | Action |
|---|---|
| `app/api/data/[type]/route.ts` | Création — route GET/PUT dynamique |
| `.gitignore` | Modification — ajout de `data/` |
| `app/todo/page.tsx` | Modification — localStorage → API |
| `app/courses/page.tsx` | Modification — localStorage → API |
| `app/radios/page.tsx` | Modification — localStorage → API |
| `app/weather/page.tsx` | Modification — localStorage → API |
| `components/TodoWidget.tsx` | Modification — localStorage → API |
| `components/CoursesWidget.tsx` | Modification — localStorage → API |
| `components/RadioWidget.tsx` | Modification — localStorage → API |

---

### Task 1 : Route API `/api/data/[type]`

**Fichiers :**
- Créer : `app/api/data/[type]/route.ts`

- [ ] **Étape 1 : Créer le dossier et le fichier**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { readFileSync, writeFileSync, mkdirSync, existsSync } from 'fs'
import path from 'path'

const VALID_TYPES = ['todos', 'courses', 'radios', 'weather']
const DATA_DIR = path.join(process.cwd(), 'data')

const DEFAULTS: Record<string, unknown> = {
  todos: [],
  courses: { items: [], history: [] },
  radios: [],
  weather: [],
}

function getFilePath(type: string) {
  return path.join(DATA_DIR, `${type}.json`)
}

export async function GET(
  _request: NextRequest,
  { params }: { params: Promise<{ type: string }> }
) {
  const { type } = await params

  if (!VALID_TYPES.includes(type)) {
    return NextResponse.json({ error: 'Type invalide' }, { status: 400 })
  }

  const filePath = getFilePath(type)

  if (!existsSync(filePath)) {
    return NextResponse.json(DEFAULTS[type])
  }

  const content = readFileSync(filePath, 'utf-8')
  return NextResponse.json(JSON.parse(content))
}

export async function PUT(
  request: NextRequest,
  { params }: { params: Promise<{ type: string }> }
) {
  const { type } = await params

  if (!VALID_TYPES.includes(type)) {
    return NextResponse.json({ error: 'Type invalide' }, { status: 400 })
  }

  const data = await request.json()

  if (!existsSync(DATA_DIR)) {
    mkdirSync(DATA_DIR, { recursive: true })
  }

  writeFileSync(getFilePath(type), JSON.stringify(data, null, 2), 'utf-8')

  return NextResponse.json({ success: true })
}
```

- [ ] **Étape 2 : Vérifier la route**

Démarrer `npm run dev`. Dans le navigateur :
- `http://localhost:3000/api/data/todos` → doit retourner `[]`
- `http://localhost:3000/api/data/courses` → doit retourner `{"items":[],"history":[]}`

- [ ] **Étape 3 : Commit**
```bash
git add app/api/data/
git commit -m "feat: add data sync API route (GET/PUT JSON files)"
```

---

### Task 2 : Mettre à jour `.gitignore`

**Fichiers :**
- Modifier : `.gitignore`

- [ ] **Étape 1 : Ajouter `data/` au `.gitignore`**

Ajouter à la fin du fichier `.gitignore` :
```
# data files (server-side storage, not in git)
data/
```

- [ ] **Étape 2 : Commit**
```bash
git add .gitignore
git commit -m "chore: ignore data/ directory from git"
```

---

### Task 3 : Mettre à jour `app/todo/page.tsx`

**Fichiers :**
- Modifier : `app/todo/page.tsx`

- [ ] **Étape 1 : Remplacer les deux useEffect de load/save**

Remplacer les deux `useEffect` (lignes 24-48) par :

```typescript
useEffect(() => {
  async function loadData() {
    const response = await fetch('/api/data/todos')
    const data = await response.json()

    if (data.length === 0) {
      const saved = localStorage.getItem('myhome-todos')
      if (saved) {
        const parsed = JSON.parse(saved)
        await fetch('/api/data/todos', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(parsed),
        })
        setTodos(parsed)
        localStorage.removeItem('myhome-todos')
        setLoaded(true)
        return
      }
    }

    setTodos(data)
    setLoaded(true)
  }
  loadData()
}, [])

useEffect(() => {
  if (!loaded) return
  fetch('/api/data/todos', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(todos),
  })
}, [todos, loaded])
```

- [ ] **Étape 2 : Vérifier**

Ouvrir `http://localhost:3000/todo`. Ajouter une tâche. Vérifier que `http://localhost:3000/api/data/todos` retourne la nouvelle tâche.

Ouvrir un autre navigateur (ou fenêtre privée) → `http://localhost:3000/todo` → la tâche doit apparaître.

- [ ] **Étape 3 : Commit**
```bash
git add app/todo/page.tsx
git commit -m "feat: todo page uses server-side JSON storage"
```

---

### Task 4 : Mettre à jour `app/courses/page.tsx`

**Fichiers :**
- Modifier : `app/courses/page.tsx`

- [ ] **Étape 1 : Remplacer le useEffect de load**

Remplacer le premier `useEffect` (lignes 27-47) par :

```typescript
useEffect(() => {
  async function loadData() {
    const response = await fetch('/api/data/courses')
    const data = await response.json()

    if (data.items.length === 0 && data.history.length === 0) {
      const savedItems = localStorage.getItem('myhome-courses')
      const savedHistory = localStorage.getItem('myhome-courses-history')
      if (savedItems || savedHistory) {
        const parsed = {
          items: savedItems ? JSON.parse(savedItems) : [],
          history: savedHistory ? JSON.parse(savedHistory) : [],
        }
        await fetch('/api/data/courses', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(parsed),
        })
        setItems(parsed.items)
        setHistory(parsed.history)
        localStorage.removeItem('myhome-courses')
        localStorage.removeItem('myhome-courses-history')
        setLoaded(true)
        return
      }
    }

    setItems(data.items)
    setHistory(data.history)
    setLoaded(true)
  }
  loadData()
}, [])
```

- [ ] **Étape 2 : Remplacer le useEffect de save**

Remplacer le second `useEffect` (lignes 49-58) par :

```typescript
useEffect(() => {
  if (!loaded) return
  fetch('/api/data/courses', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ items, history }),
  })
}, [items, history, loaded])
```

- [ ] **Étape 3 : Vérifier**

Ouvrir `http://localhost:3000/courses`. Ajouter un article. Vérifier que `http://localhost:3000/api/data/courses` retourne l'article dans `items`.

- [ ] **Étape 4 : Commit**
```bash
git add app/courses/page.tsx
git commit -m "feat: courses page uses server-side JSON storage"
```

---

### Task 5 : Mettre à jour `app/radios/page.tsx`

**Fichiers :**
- Modifier : `app/radios/page.tsx`

- [ ] **Étape 1 : Remplacer le useEffect de load (lignes 29-72)**

```typescript
useEffect(() => {
  async function loadData() {
    const response = await fetch('/api/data/radios')
    const data = await response.json()

    if (data.length === 0) {
      const saved = localStorage.getItem('myhome-radios')
      if (saved) {
        const parsed = JSON.parse(saved)
        await fetch('/api/data/radios', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(parsed),
        })
        setRadios(parsed)
        localStorage.removeItem('myhome-radios')
        return
      }

      const defaultRadios = [
        {
          name: 'Couleur 3',
          stream: 'https://stream.srg-ssr.ch/m/couleur3/mp3_128',
          logo: '/logos/couleur3.png',
          favorite: true,
        },
        {
          name: 'Reggae',
          stream: 'https://hd.lagrosseradio.info/lagrosseradio-reggae-192.mp3',
          logo: '/logos/radioreggae.png',
          favorite: true,
        },
        {
          name: 'MAXXIMA',
          stream: 'http://maxxima.mine.nu:8000/',
          logo: '/logos/maxxima.png',
          favorite: true,
        },
      ]
      await fetch('/api/data/radios', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(defaultRadios),
      })
      setRadios(defaultRadios)
      return
    }

    setRadios(data)
  }
  loadData()
}, [])
```

- [ ] **Étape 2 : Remplacer chaque `localStorage.setItem('myhome-radios', ...)` par un appel PUT**

Il y en a 2 dans la page (dans `addRadio` et d'autres mutations). Remplacer chaque occurrence par :

```typescript
await fetch('/api/data/radios', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(updatedRadios),
})
```

Note : les fonctions qui contiennent ces appels devront devenir `async`.

Garder les `localStorage.setItem('radioPlaying', ...)` — c'est l'état de lecture audio, pas des données partagées.

- [ ] **Étape 3 : Vérifier**

Ouvrir `http://localhost:3000/radios`. Les 3 radios par défaut doivent apparaître. Vérifier `http://localhost:3000/api/data/radios`.

- [ ] **Étape 4 : Commit**
```bash
git add app/radios/page.tsx
git commit -m "feat: radios page uses server-side JSON storage"
```

---

### Task 6 : Mettre à jour `app/weather/page.tsx`

**Fichiers :**
- Modifier : `app/weather/page.tsx`

- [ ] **Étape 1 : Remplacer le useEffect de load (lignes 26-43)**

```typescript
useEffect(() => {
  async function loadData() {
    const response = await fetch('/api/data/weather')
    const data = await response.json()

    if (data.length === 0) {
      const saved = localStorage.getItem('myhome-weather')
      if (saved) {
        const parsed = JSON.parse(saved)
        await fetch('/api/data/weather', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(parsed),
        })
        setCities(parsed)
        localStorage.removeItem('myhome-weather')
        setLoaded(true)
        return
      }
    }

    setCities(data)
    setLoaded(true)
  }
  loadData()
}, [])
```

- [ ] **Étape 2 : Remplacer le useEffect de save (lignes 45-56)**

```typescript
useEffect(() => {
  if (!loaded) return
  fetch('/api/data/weather', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(cities),
  })
}, [cities, loaded])
```

- [ ] **Étape 3 : Commit**
```bash
git add app/weather/page.tsx
git commit -m "feat: weather page uses server-side JSON storage"
```

---

### Task 7 : Mettre à jour `components/TodoWidget.tsx`

**Fichiers :**
- Modifier : `components/TodoWidget.tsx`

- [ ] **Étape 1 : Remplacer le useEffect**

Remplacer le `useEffect` (lignes 16-26) par :

```typescript
useEffect(() => {
  fetch('/api/data/todos')
    .then((r) => r.json())
    .then((data) => setTodos(data))
}, [])
```

- [ ] **Étape 2 : Commit**
```bash
git add components/TodoWidget.tsx
git commit -m "feat: TodoWidget reads from server API"
```

---

### Task 8 : Mettre à jour `components/CoursesWidget.tsx`

**Fichiers :**
- Modifier : `components/CoursesWidget.tsx`

- [ ] **Étape 1 : Remplacer le useEffect**

Remplacer le `useEffect` (lignes 14-31) par :

```typescript
useEffect(() => {
  fetch('/api/data/courses')
    .then((r) => r.json())
    .then((data) => {
      const remaining = data.items.filter((item: Item) => !item.checked)
      setItems(remaining)
    })
}, [])
```

- [ ] **Étape 2 : Commit**
```bash
git add components/CoursesWidget.tsx
git commit -m "feat: CoursesWidget reads from server API"
```

---

### Task 9 : Mettre à jour `components/RadioWidget.tsx`

**Fichiers :**
- Modifier : `components/RadioWidget.tsx`

- [ ] **Étape 1 : Remplacer le useEffect**

Remplacer le `useEffect` (lignes 20-36) par :

```typescript
useEffect(() => {
  fetch('/api/data/radios')
    .then((r) => r.json())
    .then((data: Radio[]) => {
      const favorites = data.filter((r) => r.favorite).slice(0, 4)
      setRadios(favorites)
    })
}, [])
```

- [ ] **Étape 2 : Commit**
```bash
git add components/RadioWidget.tsx
git commit -m "feat: RadioWidget reads from server API"
```

---

### Task 10 : Déployer sur Infomaniak

- [ ] **Étape 1 : Push**
```bash
git push
```

- [ ] **Étape 2 : SSH Infomaniak**
```bash
cd ~/sites/myhome.lpj.ch && git pull && npm run build
```

- [ ] **Étape 3 : Redémarrer** depuis le dashboard Infomaniak

- [ ] **Étape 4 : Vérifier**

Ouvrir `https://myhome.lpj.ch/todo` sur l'ordi → ajouter une tâche.
Ouvrir `https://myhome.lpj.ch/todo` sur le mobile → la tâche doit apparaître.
